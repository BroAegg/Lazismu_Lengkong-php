{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently -k -n server,queue,logs,vite -c blue,yellow,magenta,green \"php artisan serve\" \"php artisan queue:listen --tries=3\" \"php artisan pail\" \"vite\"",
        "build": "vite build",
        "vite": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
